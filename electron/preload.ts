// Minimal preload script — contextIsolation is enabled, nodeIntegration is off.
// Nothing is exposed to the renderer; the app communicates via HTTP/WS to the
// embedded Next.js server, not via IPC.
